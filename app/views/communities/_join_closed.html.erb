<%= form_for current_user.memberships.build, remote: true do |f| %>
  <%= hidden_field_tag :community_id, @community.id %>
  
  <%= f.label :access_token, "Access Code" %>
  <%= f.text_field :access_token %>

  <%= f.submit "Join" %>
<% end %>