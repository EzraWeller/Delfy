<div class="user-index">
	<p><%= link_to "Home", root_path %></p>
	<h1>User Index</h1>
	<div>
	  <% @users.each do |i| %>
	    <p>
	      <%= link_to "#{i.name}", user_path(id: i.id) %>
	    </p>
	  <% end %>
	  <%= paginate @users %>
	</div>
</div>
