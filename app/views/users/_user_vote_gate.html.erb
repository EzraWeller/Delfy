<% if v.branch_idea %>
	<div id="<%= v.branch_idea_id %>-user-vote-feed">
		<%= render 'ideas/feed_branch_idea', b: v.branch_idea %>
	</div>
<% else %>
	<div id="<%= v.idea_id %>-user-vote-feed">
		<%= render 'ideas/side_feed_root', idea: v.idea %>
	</div>
<% end %>